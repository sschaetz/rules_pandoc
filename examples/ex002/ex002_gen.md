---
title: Example 002 (generated images)
---

# Example 002 — generated images

These two SVGs are produced by `genrule`s, so they live under `bazel-out/`
rather than in the source tree.

Flat generated image (same dir as the doc):

![flat](gen_flat.svg)

Nested generated image (in a sub-dir):

![nested](sub/gen_nested.svg)
