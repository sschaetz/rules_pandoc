# Body

This document's **title**, **author**, and **date** are supplied as pandoc
*metadata* from the BUILD file, while the subtitle is a template *variable*.

Metadata flows into the document itself (the title block, the `<title>` tag,
document properties) and is visible to templates and filters. Variables only
fill `$placeholder$` slots in the template.
