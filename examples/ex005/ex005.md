---
title: Example 005
subtitle: git-sha stamping
---

# Stamping

This document is concatenated with a generated Markdown snippet that carries the
git commit SHA, stamped at build time via Bazel's workspace status. The footer
below is produced by the `git_stamp` rule in `stamp.bzl`.
